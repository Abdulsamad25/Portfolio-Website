{
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "A portfolio website for Abdulsamad",
  "scripts": {
    "start": "npx http-server ./dist",
    "build": "mkdir -p dist && if [ -d src ]; then cp -r src/* dist/; else echo 'src directory does not exist'; fi"
  },
  "author": "Abdulsamad",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.12.3"
  }
}